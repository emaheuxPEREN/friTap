[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[tool.ruff]
target-version = "py310"

[tool.ruff.lint]
# Pre-0.16 default set plus the mechanical modernisation rules; pinned, not inherited.
select = ["E4", "E7", "E9", "F", "I", "UP009", "UP032"]